# First Steps
# Create the simplest possible FastAPI file

# Step 1: Import FastAPI
# TODO: Import FastAPI from fastapi
from fastapi import FastAPI

# Step 2: Create a FastAPI instance
# TODO: Create an instance of FastAPI and assign it to a variable called 'app'
# Hint: app = FastAPI()
app = FastAPI()

# Step 3: Create a path operation
# TODO: Use the @app.get("/") decorator to create a path operation for the root path
# TODO: Define an async function below the decorator that returns {"message": "Hello World"}
# Hint: async def root():
@app.get("/")
async def root():
    return {"message": "Hello World"}
